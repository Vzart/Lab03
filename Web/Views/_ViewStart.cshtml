@{
    Layout = "_Layout";
}

@{
    var isAuthenticated = Context.Session.GetInt32("Id");

    if (isAuthenticated is null && !Context.Request.Path.StartsWithSegments("/"))
    {
        Context.Response.Redirect("/");
    }
    
    Layout = "_Layout";
}